# fastlane/Appfile
#
# Placeholders — fill these in with your own Apple Developer account
# details before running `fastlane beta`. Prefer environment variables over
# hardcoding real values here, especially in a public repo.

app_identifier("com.hetzly.app") # must match PRODUCT_BUNDLE_IDENTIFIER in project.yml
apple_id(ENV["FASTLANE_APPLE_ID"] || "you@example.com")
team_id(ENV["FASTLANE_TEAM_ID"] || "YOUR_TEAM_ID")
